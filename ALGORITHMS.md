# 자료구조와 알고리즘

## 커밋 그래프와 DAG

- `Commit` 필드: `hash`, `message`, `author`, `timestamp`, `parents`
- 저장소: `dict[hash, Commit]`, hash 기반 평균 O(1) 조회
- 자식 인접 목록 별도 관리: 무방향 탐색 시 전체 커밋 순회 방지
- DAG 유지: 기존 커밋만 부모로 허용, 새 노드에서 과거 노드로만 연결, 사이클 생성 방지
- 부모 수: 일반 커밋 0~1개, 병합 커밋 2개
- hash 구성: 증가하는 세션 카운터 전체와 SHA-256 결과 8자리 결합
- 재초기화 시 카운터 유지: 동일 REPL 세션 내 hash 중복 방지

## 부모 우선 LOG

- 탐색 방향: 저장된 `자식 → 부모` 참조의 역방향인 `부모 → 자식`
- Kahn 위상 정렬: 부모 수를 진입 차수로 사용, 차수 0 노드 처리 후 자식 차수 감소
- 출력 순서: 부모 우선, hash 최소 힙을 통한 후보 선택의 결정성 보장
- 시간복잡도: 일반 Kahn O(V+E), 현재 힙 기반 구현 O((V+E) log V) 상한
- `LOG --sort-by=...`: 위상 순서와 별개의 날짜·작성자 정렬
- Python 표준 정렬 API 대신 병합·삽입 정렬 직접 구현

| 알고리즘 | 평균 | 최악 | 추가 공간 | 안정 정렬 |
| --- | --- | --- | --- | --- |
| 병합 정렬 | O(n log n) | O(n log n) | O(n) | 예 |
| 삽입 정렬 | O(n²) | O(n²) | O(n)¹ | 예 |

¹ 정렬 루프의 추가 공간 O(1), 입력 보존용 결과 복사본 O(n)

- 정렬 옵션의 `LOG`: 병합 정렬 사용, 동률 시 왼쪽 원소 우선 선택으로 안정성 보장
- 비교 기준: 날짜 `(timestamp, hash)`, 작성자 `(소문자 author, timestamp, hash)`
- 삽입 정렬: `BENCHMARK` 비교 대상

## PATH와 ANCESTORS

| 항목 | PATH | ANCESTORS |
| --- | --- | --- |
| 탐색 대상 | 부모·자식 인접 목록의 무방향 간선 | 대상 커밋에서 부모 방향으로 도달 가능한 조상 |
| 알고리즘 | `(간선 수, 경로 hash 튜플, 현재 hash)` 우선순위 큐 | DFS 및 방문 `set` |
| 결과 순서 | 최소 간선 수 우선, 동률 시 전체 hash 경로의 사전순 | 전체 그래프 위상 정렬 후 조상만 선택 |
| 예외·중복 처리 | 미연결 시 `No path` 출력 | 동일 조상의 중복 제거 |

`ANCESTORS` 복잡도: 조상 수집 O(V+E), 전체 명령 O((V+E) log V) 시간 상한·O(V+E) 추가 공간

## 역색인

- 메시지: `split()` 토큰화 및 `lower()` 정규화 후 `keyword → commit hash 목록` 저장
- 작성자: 소문자 정규화 후 `author → commit hash 목록` 저장
- 중복 방지: 커밋별 동일 토큰의 hash 1회 등록
- 단일 토큰·작성자 검색: dict 조회 및 결과 K개 반환, 평균 O(1+K)
- 구문 검색: 토큰별 posting list 교집합 후보에서 구문 포함 여부 확인
- 검색 범위: 전체 커밋 대신 역색인 후보 조회

## DIFF

- 알고리즘: 줄 배열의 최장 공통 부분 수열(LCS), 동적 계획법
- 출력 접두사: 공통 줄 공백 2개, 삭제 줄 `- `, 추가 줄 `+ `
- 시간·공간복잡도: 파일별 줄 수 n, m 기준 O(nm)

## 알고리즘 분석 기록

- 커밋 저장소 조회 방식: hash를 key로 갖는 dict, 평균 O(1)
- LOG 부모 우선 출력 방식: 진입 차수와 최소 힙을 사용하는 Kahn 위상 정렬
- PATH 최단 경로 탐색 방식: 무방향 인접 목록과 `(거리, 전체 경로)` 우선순위 큐
- ANCESTORS 조상 탐색 방식: 부모 방향 DFS와 방문 set
- 실제 LOG 정렬 알고리즘: 안정 병합 정렬
- 정렬 평균 시간복잡도: 병합 O(n log n), 삽입 O(n²)
- 정렬 최악 시간복잡도: 병합 O(n log n), 삽입 O(n²)
- 안정 정렬 여부: 두 구현 모두 안정 정렬
- 역색인 검색 시간복잡도: 단일 key 평균 O(1+K), K는 결과 수
